# ============================================================
# Deaf Connect AI — Python 3.11 Requirements
# Clean, conflict-free, Windows-compatible
# ============================================================

# --- Core UI ---
streamlit==1.35.0
streamlit-webrtc==0.47.7

# --- Environment ---
python-dotenv==1.0.1

# --- Computer Vision & Hand Tracking ---
opencv-python==4.10.0.84        # Use ONE opencv package — headless version conflicts with GUI
mediapipe==0.10.14

# --- Deep Learning ---
# tensorflow-cpu is recommended for Windows dev machines without NVIDIA GPU
# If you have a GPU, replace with: tensorflow==2.16.1
tensorflow-cpu==2.16.1
keras==3.14.1
numpy==1.26.4

# --- AI / LLM ---
groq==1.2.0

# --- Text-to-Speech / Speech Recognition ---
# pyttsx3 is Windows-native; also using Web Speech API via JS injection
pyttsx3==2.90
SpeechRecognition==3.10.4
# PyAudio is needed by SpeechRecognition for mic input
# Install via: pip install pyaudio  (requires portaudio on Windows)
# pyaudio==0.2.14

# --- FastAPI Backend ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.27

# --- Database (SQLAlchemy + PostgreSQL/SQLite) ---
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9          # PostgreSQL driver; SQLite needs nothing extra

# --- Pydantic / Validation ---
pydantic==2.13.4
pydantic[email]
email-validator==2.3.0

# --- HTTP Client (for Streamlit→FastAPI calls) ---
requests==2.32.3
httpx==0.27.2

# --- Data / Charts ---
pandas==2.2.2
scikit-learn==1.5.0

# --- WebRTC Dependencies ---
av==12.3.0
aiortc==1.9.0

# --- Supabase (optional, only needed if using Supabase Python client directly) ---
supabase==2.5.1

# --- Utilities ---
Pillow==10.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
